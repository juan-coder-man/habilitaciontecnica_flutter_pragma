name: api_fakestore
description: Cliente Dart para Fake Store API con respaldo DummyJSON.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.8

dependencies:
  dartz: ^0.10.1
  http: ^1.2.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
